services:
  postgres:
    image: postgres:17.3
    restart: always
    shm_size: 128mb
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: vet-db
  adminer:
    image: adminer:standalone
    restart: always
    ports:
      - "8080:8080"
